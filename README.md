# Scaler_data_clustering
this project demonstrate how to build a clustering model using unsupervised clustering approach and how to validate the model's performance along with the hyperparameter tuning steps
